# Clean_HubDailyEvents Data.

# Parameters:
LS_COLS = [
	Date	CompanyCode	CompanyName	Country	HomeCountry	HomeCountryCode	Operation	EventAction	EventCategory	EventLabel	EventSection	EventQuestion	UserType	Users	TotalEvents	UniqueEvents	SessionswithEvent	Events/SessionwithEvent
]

LS_STR_COLS = [CompanyCode	CompanyName	Country	HomeCountry	HomeCountryCode	Operation	EventAction	EventCategory	EventLabel	EventSection	EventQuestion	UserType]

LS_INT_COLS = [TotalEvents	UniqueEvents	SessionsWithEvent	Events/SessionwithEvent]

sort_order = ['Date', 'CompanyCode', 'CompanyName', 'Country', 'HomeCountry', 'HomeCountryCode']


[x] Read in the data using the following options while bearing in mind that the .csv uses a semicolon ";" as the field delimiter:  engine='python', escapechar='\\'
	Read with the "utf-8" encoding.

[x] Rename the columns as follows:
	eventAction   -> EventAction
	eventCategory -> EventCategory
	eventLabel    -> EventLabel
	eventSection  -> EventSection
	eventQuestion -> EventQuestion
	EventsPerSessionWithEvent -> Events/SessionwithEvent
	

[x] Locate the input file `HubDailyEventData_yyyy-mm-dd.csv"

[x] Derive the `month-tag` from the filename, and subtract 1 month from it.  Store the result inside `year`, `month` variables respectively.
    Concatenate `year` and `month` into a single string: `month_tag` = <year><month>.

[x] The Data Columns need to be arranged in the order specified in LS_COLS. Drop any other columns that are not contained in `LS_COLS`.

[x] Between the columns `Operation` and `EventAction`, insert a column called `Feature`.

[x] Convert `Date` entries to the following format: ("%Y-%m-%d %H:%M:%S.%f").str[:-3]
	Example output for this would look like:  "2026-07-13 00:00:00.000"

[x] Strip any HTML tags, entities, HTML attributes from the `EventSection` column.

[x] Trim any redundant white space (trailing space, leading space, double spaces, tabs), .str.strip(), in the string fields specified in LS_STR_COLS.

[x] Please also collapse any Double white space into a single white space for the fields specified in LS_STR_COLS.

[x] Remove any rows that are completely blank.

[x] Remove any rows where the `Date` entry is blank/missing.

[x] Cast the columns specified in LS_INT_COLS to integer format.

[x] Sort the rows in Ascending (or A->Z) order based on columns specified in `sort_order`.


# Save the dataset
Look at the filename of the input data.

Save and store the dataset as: HubDailyEvents_<month_tag>_cleaned.csv
So, as an example, the input of HubDailyEvents_2026-08-02.csv would lead to the output: HubDailyEvents_202607_cleaned.csv.
Use ";" as the field delimiter.

[x] Write a Summary Report.